import 'package:flutter/material.dart';

class AppsToUpdate extends StatelessWidget {
  const AppsToUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
