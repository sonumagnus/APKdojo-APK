import 'package:flutter/material.dart';

class ShareModelFixed extends StatelessWidget {
  const ShareModelFixed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
